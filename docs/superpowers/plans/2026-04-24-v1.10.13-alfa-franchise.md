# v1.10.13 Alfa per-row Франшиза comment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** For Alfa xlsx files, when a per-row "Вид медицинского обслуживания" (or equivalent) cell contains the keyword "Франшиза", the full cell text lands in that record's `Комментарий в полис` column in master.xlsx, the daily delta, and the network CSVs.

**Architecture:** Two tight edits — the Alfa parser gains a comment-column detection pass that reuses `clinic_matcher._COMMENT_COLUMNS` and a per-row "Франшиза" check; `main.py:process_file` uses `r.get('Комментарий в полис') or comment` so parser-set values win over the clinic_matcher fallback. All other insurers unchanged.

**Tech Stack:** Python 3.11+, pytest, openpyxl, pandas. Reuses existing `_COMMENT_COLUMNS` list from `clinic_matcher.py`.

**Spec:** `docs/superpowers/specs/2026-04-24-alfa-franchise-comment-v1.10.13-design.md`

---

## Task 1: Alfa parser — detect comment column + per-row Франшиза extraction

**Files:**
- Modify: `parsers/alfa.py` (import `_COMMENT_COLUMNS`; add `col_comment` detection; per-row write)
- Create: `tests/test_alfa_franchise.py`

### Step 1.1: Write failing tests

Create `C:\Pyth\email-processor\tests\test_alfa_franchise.py`:

```python
"""Per-row Франшиза comment extraction for Alfa parser (v1.10.13)."""
from openpyxl import Workbook
from parsers.alfa import parse


def _write_alfa_xlsx(path, data_rows, *, header_row_idx=6, include_comment_col=True):
    """Build a minimal Alfa-format xlsx.

    data_rows: list of (num, polis, fio, birth, start, end, comment) tuples.
    include_comment_col: if False, omit the 'Вид медицинского обслуживания' header
    """
    wb = Workbook()
    ws = wb.active
    # Title/header area (Alfa-style)
    ws.cell(row=1, column=1, value='АО "АльфаСтрахование"')
    ws.cell(row=4, column=1, value='Список застрахованных')
    ws.cell(row=5, column=1, value='для обслуживания в сети клиник ________')
    # Column headers at header_row_idx + 1 (openpyxl is 1-indexed)
    header_row = header_row_idx + 1
    ws.cell(row=header_row, column=1, value='№ п/п')
    ws.cell(row=header_row, column=2, value='№ полиса')
    ws.cell(row=header_row, column=3, value='ФИО')
    ws.cell(row=header_row, column=4, value='Дата рождения')
    ws.cell(row=header_row, column=5, value='Адрес')
    ws.cell(row=header_row, column=6, value='Группа, № договора')
    ws.cell(row=header_row, column=7, value='Период с')
    ws.cell(row=header_row, column=8, value='Период по')
    if include_comment_col:
        ws.cell(row=header_row, column=9, value='Вид медицинского обслуживания')
    # Data rows start after header
    for i, (num, polis, fio, birth, start, end, comment) in enumerate(data_rows, start=header_row + 1):
        ws.cell(row=i, column=1, value=num)
        ws.cell(row=i, column=2, value=polis)
        ws.cell(row=i, column=3, value=fio)
        ws.cell(row=i, column=4, value=birth)
        ws.cell(row=i, column=6, value='Группа X; №12345; ООО "ТестСтрах"')
        ws.cell(row=i, column=7, value=start)
        ws.cell(row=i, column=8, value=end)
        if include_comment_col and comment is not None:
            ws.cell(row=i, column=9, value=comment)
    wb.save(path)


def test_alfa_extracts_franchise_per_row(tmp_path):
    """Different Франшиза values per-row land in each record's Комментарий в полис."""
    path = tmp_path / "alfa_multi.xlsx"
    _write_alfa_xlsx(path, [
        (1, 'POL001', 'ИВАНОВ И И', '01.01.1990', '01.01.2026', '31.12.2026',
         'Франшиза 20%: амбулаторно-поликлиническое'),
        (2, 'POL002', 'ПЕТРОВ П П', '02.02.1985', '01.01.2026', '31.12.2026',
         'Франшиза 45%: амбулаторно + стационар'),
        (3, 'POL003', 'СИДОРОВ С С', '03.03.1980', '01.01.2026', '31.12.2026',
         'Без франшизы, амбулаторно'),
    ])

    records = parse(str(path))

    assert len(records) == 3
    assert records[0]['Комментарий в полис'] == 'Франшиза 20%: амбулаторно-поликлиническое'
    assert records[1]['Комментарий в полис'] == 'Франшиза 45%: амбулаторно + стационар'
    # "Без франшизы" — "франшизы" (genitive) does NOT contain "франшиза" as substring,
    # so parser does not set the field. Absent key OR empty/None.
    assert not records[2].get('Комментарий в полис')


def test_alfa_no_comment_column_no_extraction(tmp_path):
    """If comment column header is absent, parser does not set Комментарий в полис."""
    path = tmp_path / "alfa_no_comment_col.xlsx"
    _write_alfa_xlsx(path, [
        (1, 'POL001', 'ИВАНОВ И И', '01.01.1990', '01.01.2026', '31.12.2026', None),
    ], include_comment_col=False)

    records = parse(str(path))

    assert len(records) == 1
    assert not records[0].get('Комментарий в полис')


def test_alfa_franchise_cell_uppercase(tmp_path):
    """Franchise keyword match is case-insensitive."""
    path = tmp_path / "alfa_upper.xlsx"
    _write_alfa_xlsx(path, [
        (1, 'POL001', 'ИВАНОВ И И', '01.01.1990', '01.01.2026', '31.12.2026',
         'ФРАНШИЗА 30%: амбулаторно'),
    ])

    records = parse(str(path))

    assert len(records) == 1
    # Original (non-lowercased) text preserved
    assert records[0]['Комментарий в полис'] == 'ФРАНШИЗА 30%: амбулаторно'


def test_alfa_non_franchise_comment_not_extracted(tmp_path):
    """A comment cell WITHOUT 'франшиза' is not picked up by the parser.
    (This is clinic_matcher's job via main.py fallback — parser stays Alfa-specific to франшиза.)"""
    path = tmp_path / "alfa_no_franchise.xlsx"
    _write_alfa_xlsx(path, [
        (1, 'POL001', 'ИВАНОВ И И', '01.01.1990', '01.01.2026', '31.12.2026',
         'Амбулаторно-поликлиническое обслуживание без стоматологии'),
    ])

    records = parse(str(path))

    assert len(records) == 1
    assert not records[0].get('Комментарий в полис')
```

### Step 1.2: Verify tests FAIL

```bash
python -m pytest tests/test_alfa_franchise.py -v
```

Expected: 4 FAILS. `test_alfa_extracts_franchise_per_row` will fail because records don't have `Комментарий в полис`. `test_alfa_no_comment_column_no_extraction`, `test_alfa_non_franchise_comment_not_extracted` will pass already (parser doesn't set the key today — `not records[0].get(...)` is True). `test_alfa_franchise_cell_uppercase` will fail same reason as test 1.

**Expected failures:** 2 (tests 1 and 3). **Expected passes:** 2 (tests 2 and 4). If the numbers don't match, stop and report.

### Step 1.3: Apply fix — update imports in `parsers/alfa.py`

Current import (line 15):
```python
from parsers.utils import format_date, get_cell_str
```

Change to:
```python
from parsers.utils import format_date, get_cell_str
from clinic_matcher import _COMMENT_COLUMNS
```

### Step 1.4: Apply fix — detect `col_comment` during header scan

In `parsers/alfa.py`, locate the header-detection loop (currently around lines 36-78). After the existing `col_*` detection (num, polis, fio, birth, group, start, end), also populate `col_comment`. Add `col_comment = None` to the defaults block (lines 25-32), and inside the `for ci in range(len(df.columns))` header scan (around line 41-67), add one more `elif` branch checking if the lowercased header cell matches any entry in `_COMMENT_COLUMNS`.

Full replacement for the defaults block (currently lines 25-32):

```python
    # Defaults (overridden by header detection below)
    col_num = 0
    col_polis = 1
    col_fio = 2
    col_birth = 3
    col_group = 5
    col_start = 6
    col_end = 7
    col_comment = None  # set only if comment-column header matches _COMMENT_COLUMNS
```

Inside the header-scan `for ci` loop, right after the existing `elif 'дата' in h and 'прикреплен' in h:` branch and BEFORE the inner-closing code, add:

```python
                # Match comment-column headers (shared with clinic_matcher)
                elif any(kw in h for kw in _COMMENT_COLUMNS):
                    col_comment = ci
```

The full header-detection block after edit (lines 41-67) becomes:

```python
            for ci in range(len(df.columns)):
                val = df.iloc[hi, ci]
                if pd.isna(val):
                    continue
                h = str(val).strip().lower()
                if 'п/п' in h:
                    col_num = ci
                elif 'полис' in h:
                    col_polis = ci
                elif 'фио' in h:
                    col_fio = ci
                elif 'дата' in h and 'рожд' in h:
                    col_birth = ci
                elif 'группа' in h or 'договор' in h or 'организац' in h:
                    col_group = ci
                # Handle merged date headers: "Дата открепления с" / "Дата открепления по"
                # or "Период с" / "Период по" as single cells
                elif ('дата' in h or 'период' in h) and h.rstrip().endswith(' с'):
                    col_start = ci
                elif ('дата' in h or 'период' in h) and h.rstrip().endswith(' по'):
                    col_end = ci
                # "Дата открепления" without с/по — treat as end date (открепление = detachment)
                elif 'дата' in h and 'откреплен' in h:
                    col_end = ci
                # "Дата прикрепления" — treat as start date
                elif 'дата' in h and 'прикреплен' in h:
                    col_start = ci
                # Match comment-column headers (shared with clinic_matcher)
                elif any(kw in h for kw in _COMMENT_COLUMNS):
                    col_comment = ci
```

### Step 1.5: Apply fix — per-row franchise extraction

In the per-row loop (currently around lines 85-128), after the record dict is built and before `results.append(record)`, add the franchise check:

```python
            record = {
                'ФИО': fio.upper(),
                'Дата рождения': birth,
                '№ полиса': polis,
                'Начало обслуживания': start_date,
                'Конец обслуживания': end_date,
                'Страховая компания': 'АльфаСтрахование',
                'Страхователь': strahovatel,
            }
            # Per-row franchise comment (v1.10.13): if this row's comment column contains
            # "Франшиза" (case-insensitive), capture the full cell text for Комментарий в полис.
            if col_comment is not None:
                cell = get_cell_str(df, i, col_comment)
                if cell and 'франшиза' in cell.lower():
                    record['Комментарий в полис'] = cell
            results.append(record)
```

### Step 1.6: Verify tests PASS

```bash
python -m pytest tests/test_alfa_franchise.py -v
```

Expected: 4 PASSED.

### Step 1.7: Run full parser suite + existing alfa tests (if any in test_parsers.py)

```bash
python -m pytest tests/test_parsers.py tests/test_alfa_franchise.py -v
```

Expected: all PASS or SKIP. Any fixture-dependent skips are normal.

### Step 1.8: Run full suite

```bash
python -m pytest tests/ --tb=short 2>&1 | tail -5
```

Expected: 193 pass / 17 skip (was 189 + 4 new).

### Step 1.9: Commit

Stage ONLY `parsers/alfa.py` and `tests/test_alfa_franchise.py`:

```bash
git add parsers/alfa.py tests/test_alfa_franchise.py
git commit -m "$(cat <<'EOF'
feat: Alfa per-row Франшиза comment extraction (v1.10.13)

When an Alfa xlsx has a comment-column header matching any of the
shared _COMMENT_COLUMNS entries (e.g. "Вид медицинского обслуживания"),
the parser now reads each row's cell in that column. If the cell
contains "Франшиза" (case-insensitive), the full cell text lands in
record['Комментарий в полис'] for that patient.

Cells without "Франшиза" are not extracted — clinic_matcher's
file-level fallback still handles those via main.py integration
(next commit).

Part of v1.10.13 spec.
EOF
)"
```

**DO NOT PUSH.**

---

## Task 2: main.py — preserve parser-set `Комментарий в полис` over clinic_matcher fallback

**Files:**
- Modify: `main.py:268` (one-line change)
- Test: `tests/test_alfa_franchise.py` (add 2 integration tests)

### Step 2.1: Write failing integration tests

Append to `tests/test_alfa_franchise.py`:

```python
class TestMainCommentPrecedence:
    """Parser-set Комментарий в полис wins over clinic_matcher fallback."""

    def test_parser_franchise_wins_over_clinic_matcher(self, tmp_path, monkeypatch):
        """An Alfa file with per-row Франшиза AND a clinic-matched extract_comment flag:
        the parser's per-row value must win."""
        import main
        from unittest.mock import patch

        path = tmp_path / "alfa.xlsx"
        _write_alfa_xlsx(path, [
            (1, 'POL001', 'ИВАНОВ И И', '01.01.1990', '01.01.2026', '31.12.2026',
             'Франшиза 55%: per-row value'),
        ])

        # Force clinic_matcher to return a clinic with extract_comment=True and a
        # distinct file-level comment that should lose to parser's per-row value.
        monkeypatch.setattr(main, 'detect_clinic',
                            lambda *a, **kw: ('Клиника1', True, '123'))
        monkeypatch.setattr(main, 'extract_policy_comment',
                            lambda *a, **kw: 'FILE-LEVEL-CLINIC-COMMENT')

        stats = main.make_stats()
        stats['master_path'] = str(tmp_path / 'master.xlsx')
        config = {'processing': {'deduplicate': False}}
        existing_keys = set()

        main.process_file(str(path), stats['master_path'], config, stats,
                         existing_keys=existing_keys, dry_run=True, pending=None)

        assert len(stats['new_records']) == 1
        assert stats['new_records'][0]['Комментарий в полис'] == 'Франшиза 55%: per-row value'


    def test_clinic_matcher_fills_when_parser_empty(self, tmp_path, monkeypatch):
        """When the parser sets no Комментарий (no Франшиза in file), clinic_matcher's
        file-level fallback still populates the field."""
        import main

        path = tmp_path / "alfa_no_franchise.xlsx"
        _write_alfa_xlsx(path, [
            (1, 'POL001', 'ИВАНОВ И И', '01.01.1990', '01.01.2026', '31.12.2026',
             'Амбулаторно-поликлиническое без стоматологии'),
        ])

        monkeypatch.setattr(main, 'detect_clinic',
                            lambda *a, **kw: ('Клиника1', True, '123'))
        monkeypatch.setattr(main, 'extract_policy_comment',
                            lambda *a, **kw: 'CLINIC-MATCHER-FALLBACK')

        stats = main.make_stats()
        stats['master_path'] = str(tmp_path / 'master.xlsx')
        config = {'processing': {'deduplicate': False}}

        main.process_file(str(path), stats['master_path'], config, stats,
                         existing_keys=set(), dry_run=True, pending=None)

        assert len(stats['new_records']) == 1
        assert stats['new_records'][0]['Комментарий в полис'] == 'CLINIC-MATCHER-FALLBACK'
```

### Step 2.2: Verify tests FAIL

```bash
python -m pytest tests/test_alfa_franchise.py::TestMainCommentPrecedence -v
```

Expected: `test_parser_franchise_wins_over_clinic_matcher` FAILS (main.py unconditionally overwrites with 'FILE-LEVEL-CLINIC-COMMENT'). `test_clinic_matcher_fills_when_parser_empty` passes already (the current behavior DOES fill from clinic_matcher).

### Step 2.3: Apply fix to main.py:268

The current line:

```python
    records = [{**r, 'Клиника': clinic, 'ID Клиники': clinic_id, 'Комментарий в полис': comment} for r in records]
```

Replace with:

```python
    records = [{
        **r,
        'Клиника': clinic,
        'ID Клиники': clinic_id,
        'Комментарий в полис': r.get('Комментарий в полис') or comment,
    } for r in records]
```

**Why this works:**
- `**r` unpacks parser output first (including any per-row `Комментарий в полис` it set).
- The three explicit keys are applied after `**r` so they win over any accidental parser-set `Клиника` / `ID Клиники`.
- `'Комментарий в полис': r.get('Комментарий в полис') or comment` — if the parser set a non-empty string, that wins; otherwise fall back to clinic_matcher's `comment` (which may also be empty).

### Step 2.4: Verify tests PASS

```bash
python -m pytest tests/test_alfa_franchise.py -v
```

Expected: all 6 PASS (4 parser + 2 integration).

### Step 2.5: Run full suite

```bash
python -m pytest tests/ --tb=short 2>&1 | tail -5
```

Expected: 195 pass / 17 skip (was 193 + 2 new). Zero regressions.

Watch carefully: if any of `test_main.py` / `test_dedup.py` / `test_writer.py` / `test_pipeline_resilience.py` fails, it likely means a test was asserting `Комментарий в полис` comes ONLY from clinic_matcher. In that case the test was asserting stale behavior — fix it to match the new precedence (parser wins).

### Step 2.6: Commit

```bash
git add main.py tests/test_alfa_franchise.py
git commit -m "$(cat <<'EOF'
feat: main.py preserves parser-set Комментарий в полис (v1.10.13)

process_file now uses `r.get('Комментарий в полис') or comment` when
assembling records, so a parser-set per-row value wins over
clinic_matcher's file-level extract_policy_comment fallback.

Today only Alfa's parser sets the field (for rows containing
"Франшиза" — previous commit). Other insurers are unaffected.

Part of v1.10.13 spec.
EOF
)"
```

**DO NOT PUSH.**

---

## Task 3: v1.10.13 release — version bump, CHANGELOG, tag, push

**Files:**
- Modify: `main.py:12` (`__version__`)
- Modify: `CHANGELOG.md` (prepend v1.10.13 entry)

### Step 3.1: Bump version

Edit `main.py:12`:

```python
# BEFORE:
__version__ = "1.10.12"

# AFTER:
__version__ = "1.10.13"
```

### Step 3.2: Add CHANGELOG entry

Prepend in `CHANGELOG.md` immediately after `# Changelog` and before the `## [1.10.12]` heading:

```markdown
## [1.10.13] - 2026-04-24
### Added
- **Alfa per-row Франшиза comment**: when an Alfa xlsx has a comment-column header (any of `_COMMENT_COLUMNS` — "Вид медицинского обслуживания", "Программа ДМС", etc.), each row's cell in that column is checked for the keyword "Франшиза" (case-insensitive). If found, the full cell text lands in `record['Комментарий в полис']`. Flows through to `master.xlsx`, the daily delta xlsx attached to email, and the daily/monthly CSVs on the network share. Fixes cases where Alfa files with blanked clinic names ("сети клиник ________") previously silently lost this financial info.

### Changed
- `main.py:process_file` now uses `r.get('Комментарий в полис') or comment` when assembling records, so parser-set per-row values win over `clinic_matcher.extract_policy_comment`'s file-level fallback. Today only the Alfa parser sets the field; other insurers retain the previous file-level behavior.
```

### Step 3.3: Pre-push drift check

Before committing the release, verify no subagent pushed ahead of us:

```bash
git fetch origin
git log HEAD..origin/main --oneline
```

Expected: empty output. If non-empty, STOP and flag — same subagent-push-without-permission scenario from v1.10.11 / v1.10.12.

### Step 3.4: Commit release

```bash
git add main.py CHANGELOG.md docs/superpowers/plans/2026-04-24-v1.10.13-alfa-franchise.md
git commit -m "$(cat <<'EOF'
chore: v1.10.13 — Alfa per-row Франшиза comment extraction

See CHANGELOG.md for details. One focused feature:

- parsers/alfa.py: per-row comment-column detection + Франшиза capture
- main.py:process_file: preserve parser-set Комментарий в полис over
  clinic_matcher fallback

Non-Alfa parsers unchanged. Schema unchanged (existing column reused).
EOF
)"
```

### Step 3.5: Tag

```bash
git tag v1.10.13
```

### Step 3.6: Push main + tag

```bash
git push origin main
git push origin v1.10.13
```

If push is rejected (non-fast-forward), STOP and ask the controller — this is the subagent-drift recovery scenario.

### Step 3.7: Remind user to deploy

```bash
# On VM:
git pull
python3 -c "import main; print(main.__version__)"  # should print 1.10.13

# Verify after next Alfa email arrives and gets processed:
grep "Франшиза" logs/processor.log  # should NOT show errors
grep -E "alfa.*parsed" logs/processor.log | tail -3  # parser activity

# The master.xlsx Комментарий в полис column should now carry per-row
# Франшиза text for Alfa rows. Check in Excel or:
python3 -c "
import pandas as pd
df = pd.read_excel('./output/master.xlsx', dtype=str).fillna('')
alfa = df[df['Страховая компания'].str.contains('льфа', na=False)]
with_comment = alfa[alfa['Комментарий в полис'].str.contains('ранш', na=False)]
print(f'Alfa rows: {len(alfa)}, with Франшиза comment: {len(with_comment)}')"
```

---

## Verification checklist

After all tasks complete:

- [ ] `python -m pytest tests/ --tb=short` — 195 pass / 17 skip (no regressions)
- [ ] `git log --oneline v1.10.12..HEAD` — 3 commits: Task 1 feat, Task 2 feat, Task 3 chore
- [ ] `git tag -l 'v1.10.*'` — includes v1.10.13
- [ ] Manual smoke test: `python main.py --test ./test_files` and verify the deploy.xlsx file's rows carry `Франшиза 45.00%: ...` in the preview (run output should include `Комментарий в полис` per row).
- [ ] Post-deploy on VM: a real Alfa email with Франшиза rows produces a daily delta xlsx whose `Комментарий в полис` column is populated per-row.

## Out of scope for this release

- Extending Франшиза extraction to other insurers (VSK, RESO, Zetta, etc.). If needed later, each would get its own parser-level edit.
- Changing `_COMMENT_COLUMNS` keyword list. If a new Alfa header phrasing appears, add it there (shared with clinic_matcher).
- Stem matching (`"франшиз"` to also catch `"франшизы"`). Current spec locks to `"франшиза"` exact; flip only if observed missing a known Alfa phrasing.
- Schema expansion beyond `Комментарий в полис`. That column existed pre-v1.10.13; this release only populates it more often.
